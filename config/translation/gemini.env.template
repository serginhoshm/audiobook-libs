# Fonte primaria de chave: config/pipeline.ini em [api_keys] gemini_api_key.
# Este arquivo e apenas fallback legado para compatibilidade.
# O arquivo gemini.env esta no .gitignore e nao deve ser versionado.

GEMINI_API_KEY=coloque_sua_chave_aqui
# Modelo padrao para traducao e prompt geral.
GEMINI_MODEL=gemini-1.5-flash
